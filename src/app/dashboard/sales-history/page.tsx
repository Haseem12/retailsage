import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function SalesHistoryPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Sales History</CardTitle>
        <CardDescription>This is a placeholder page for viewing sales history.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Sales history features will be implemented here.</p>
      </CardContent>
    </Card>
  );
}
